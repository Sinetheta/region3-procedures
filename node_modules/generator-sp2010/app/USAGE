Description:
    Creates a new SharePoint 2010 project with RESTful express server.

Options:
    Twitter Bootstrap: Include Twitter Bootstrap
    jQuery UI: Include jQuery UI

Example:
    yo sp2010

    This will create:
        Gruntfile.js: Configuration for the task runner.
        component.json: Front-end packages installed by bower.
        package.json: Development packages installed by npm.

        app/: Your application files.