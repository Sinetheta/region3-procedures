'use strict';

module.exports = function(grunt) {

  grunt.initConfig({
    sp2010: {
      options: {
        port: <%= port %>,
        base: 'app',
        load: 'app/lists',
        keepalive: true
      },
      gruntfile: {
        src: 'Gruntfile.js'
      }
    }
  });

  grunt.loadNpmTasks('grunt-sp2010');

  grunt.registerTask('default', ['sp2010']);
};
